spring:
  datasource:
    url: DATABASE_URL
    username: DATABASE_USERNAME
    password: DATABASE_PASSWORD
    schema: DATABASE_SCHEMA
  flyway:
    baseline-description: << Schema Tst Flyway >>
    schemas: assembly

  activemq:
    broker-url: CLOUDAMQP_URL
    password: CLOUDAMQP_APIKEY
