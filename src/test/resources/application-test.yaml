spring:
  datasource:
    data : classpath*:db/
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    baseline-description: << Schema Test Flyway >>
  h2:
    console:
      enabled: true
      path: /h2